name: my_portfolio
description: "A Glassmorphism Flutter Portfolio App"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.1.5+1
  glassmorphism: ^3.0.0
  google_fonts: ^8.0.2
  font_awesome_flutter:
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  # assets:
  #   - assets/
  #   - assets/images/
  assets:
    - assets/cv.pdf
